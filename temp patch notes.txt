Removed PIXI and replaced with WebGL2 renderer. This was because we're unable to actually get true 3D using PIXI.

Fixed 3D angle perspective on maps.

Saving and exporting game as a file now opens up a Windows save menu, allowing you to select where to export to and what file name to call it. 

You can now see events (faded) on all layers instead of only on the event layer. Event Layer still shows full opacity events.

Pressing F now works to swing your basic attack. (Same way J did.)

Action Combat got an enemy AI update, so enemies can now chase and damage the player.
Added damage pop-ups and screen shake when the player is hit.

Rebuild the desktop client. 

-Drifty
