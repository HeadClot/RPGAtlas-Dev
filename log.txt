1.1 Added quest data to project defaults and migration so older projects load with `quests: []`. (`js/data.js`)
1.2 Added runtime quest state to new-game setup and save/load handling with old-save fallback. (`js/engine.js`)
1.3 Added a `Quests` Database tab with add/delete/reorder controls and basic fields for name, description, category, visibility, rewards, and next quests. (`js/editor.js`)
1.4 Expanded the quest editor with title, short/long descriptions, start requirements, fail effects, next quest IDs, and an auto-start-next toggle. (`js/editor.js`)
1.5 Added a quest runtime helper API for querying, starting, completing, and failing quests with guarded state transitions and one-time reward/effect application. (`js/engine.js`)
1.6 Added quest event controls to the editor and runtime: Start Quest, Complete Quest, Fail Quest, and Quest Status conditions in branches, with summaries and script help updated. (`js/editor.js`, `js/engine.js`)
1.7 Added an explicit quest reward handler for completion rewards so quests can grant XP, gold, and items, including multiple rewards, with one-time payout protection. (`js/engine.js`)
1.8 Added a Journal entry to the pause menu with Active, Completed, and Failed quest views, quest detail windows, and graceful empty-state messaging. (`js/engine.js`)
1.9 Added quest chain unlocking so completed quests can unlock follow-up quests exactly once, with optional auto-start or inactive availability state for the next quests. (`js/engine.js`)
1.10 Added bundled sample quest content: one event-driven quest and one fetch-style quest, both wired into the sample map events so they can be started, tracked in the Journal, and completed in playtest. (`js/data.js`)
1.11 Added quest reward notifications so completing a quest can immediately show a message summarizing the granted rewards. (`js/engine.js`)

2.1 Added quest objective definitions to the data model with normalized `objectives` arrays supporting `event`, `kill`, and `fetch` kinds across editor, sample data, and migration. (`js/data.js`, `js/editor.js`, `js/engine.js`)
2.2 Expanded the Quests tab with built-in objective templates for Event, Kill, and Fetch objectives, showing only the relevant fields for each template while keeping quest data normalized. (`js/editor.js`)
2.3 Updated the Journal to show objective lines with progress counters, dim/check completed objectives, and show failed-outcome text/effects when present. (`js/engine.js`)
2.4 Added automatic kill tracking in battle so enemy deaths advance matching active kill objectives correctly, including multi-enemy battles and save-persistent progress. (`js/engine.js`)
2.5 Added automatic fetch objective evaluation using live inventory counts, with quest completion validating fetch turn-ins against configured target map/event flow. (`js/engine.js`)
2.6 Added a fetch consume-on-complete toggle so fetch objectives can optionally remove the turned-in items, while non-consuming fetch objectives leave inventory unchanged. (`js/engine.js`, `js/editor.js`, `js/data.js`)
2.7 Added explicit objective progress event commands for advancing or setting quest objective progress so designers can override automatic tracking when needed. (`js/editor.js`, `js/engine.js`)
2.8 Added event-linked objective completion support by letting event objectives be completed through the new objective progress commands and surfacing that progress in the Journal. (`js/editor.js`, `js/engine.js`, `js/data.js`)
2.9 Added sample automatic objective content: a kill-count wolf hunt and an objective-driven fetch quest with consume-on-complete turn-in, both playable without custom scripts. (`js/data.js`, `js/engine.js`)
2.10 Added Quest Status as a native event page condition in both the page editor UI and runtime page activation checks. (`js/editor.js`, `js/engine.js`, `js/data.js`)
2.11 Added quest objective page conditions so event pages can react to a specific objective being incomplete or completed before the overall quest is turned in. (`js/editor.js`, `js/engine.js`, `js/data.js`)

3.1 Added a quest fail-condition model with persisted/migrated data for manual fail, switch/variable fail, troop-loss fail, and defeat-count fail conditions. (`js/data.js`, `js/engine.js`)
3.2 Expanded the Quests editor with fail-condition editing, richer fail effects (including quest unlock/lock), and designer-facing failure text. (`js/editor.js`)
3.3 Added battle defeat tracking for failure conditions so relevant battle losses increment failure progress and can fail quests exactly once. (`js/engine.js`)
3.4 Added automatic failure evaluation after battle losses, switch/variable changes, quest/objective updates, and other relevant state changes, with failed quests blocked from completion unless restarted by policy. (`js/engine.js`)
3.5 Added failed-quest journal messaging so failed quests remain distinguishable and can show consequence/final outcome text. (`js/engine.js`)
3.6 Extended availability/prerequisite behavior through gated quest starts, failed-quest prerequisites, and quest lock/unlock effects for cleaner branching. (`js/editor.js`, `js/engine.js`, `js/data.js`)
3.7 Added sample failable branch content with Wolf Hunt failure consequences and a follow-up quest reflecting that outcome. (`js/data.js`)
3.8 Added restart/abandon policy fields and enforcement, including optional player abandonment from the Journal and restart-on-fail gating in the runtime. (`js/editor.js`, `js/engine.js`, `js/data.js`)
3.9 Refined the Journal abandon UX so active quests open directly to their details, with an Abandon action presented on the detail screen itself. (`js/engine.js`)
3.10 Polished the Journal detail dialog with directional button navigation, centered positioning, and immediate Journal close after abandoning a quest. (`js/engine.js`)
3.11 Fixed abandoned/failed quest state capture so objective progress is snapshotted when a quest ends, keeping the archived objective statuses stable afterward. (`js/engine.js`)
3.12 Fixed objective-based event page conditions so they only match while the quest is active, preventing abandoned quests from looking turn-in-ready. (`js/engine.js`)
