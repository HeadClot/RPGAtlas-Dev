/* RPGAtlas — src/engine/scenes/battle-fx.ts
   Battle visual effects. The implementation moved verbatim to
   src/shared/battle-fx.ts in Phase 5 Stage A (so the animation player and the
   editor's animation preview can drive the same primitives); this module
   keeps the engine-side import path stable. GPL-3.0-or-later (see LICENSE). */

export { createBattleFx } from "../../shared/battle-fx.js";
